# Explicit development catalog only. Simulate and explain are disabled in mock mode.
VITE_MOCK_MODE=false
# Optional public API origin. Empty uses the same origin and Vite /api proxy.
VITE_API_BASE_URL=
